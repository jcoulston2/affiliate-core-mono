//@flow
import React from 'react';
import CmsGrid from '@units/CmsGrid';
import { type CmsGridContent } from '@types/cms';

export default function MarketingBanner(props: CmsGridContent) {
  return <CmsGrid {...props} />;
}
