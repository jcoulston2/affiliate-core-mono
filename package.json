{
  "name": "affiliate-mono",
  "private": true,
  "scripts": {
    "boot": "lerna bootstrap --hoist && lerna run compile --stream",
    "prepare": "npm install && lerna bootstrap --hoist && lerna run compile --stream",
    "compile": "lerna run compile --stream",
    "build-schemas": "lerna run build-schemas --stream && npm run boot",
    "new-schema": "lerna run new-schema --stream",
    "start-crawl": "lerna run start-crawl --stream && npm run build-predictive-search && npm run boot",
    "build-predictive-search": "lerna run build-predictive-search --stream",
    "pull-page-mocks": "lerna run pull-page-mocks --stream",
    "test": "lerna run test --stream",
    "app:gen-site-map": "lerna run gen-site-map --stream",
    "builder:test-e2e": "lerna run test-e2e --stream",
    "crawler:test-e2e": "lerna run test-e2e --stream",
    "link": "lerna link",
    "dev": "lerna run dev --stream",
    "start": "lerna run start --stream",
    "build": "npm run boot && lerna run build --stream && node post-build.js",
    "log-store-cache": "lerna run log-store-cache --stream"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.5",
    "@babel/core": "^7.11.1",
    "@babel/node": "^7.10.5",
    "@babel/plugin-proposal-object-rest-spread": "^7.11.0",
    "@babel/plugin-transform-runtime": "^7.12.1",
    "@babel/preset-env": "^7.11.0",
    "@babel/preset-flow": "^7.10.4",
    "@babel/runtime": "^7.11.2",
    "@svgr/webpack": "^5.4.0",
    "@types/node": "^13.13.15",
    "babel-core": "^7.0.0-bridge.0",
    "babel-eslint": "^10.1.0",
    "babel-loader": "^8.2.2",
    "babel-plugin-require-context-hook": "^1.0.0",
    "babel-plugin-styled-components": "^1.11.1",
    "babel-plugin-transform-class-properties": "^6.24.1",
    "babel-preset-env": "^1.7.0",
    "eslint": "^6.6.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-loader": "^3.0.4",
    "eslint-plugin-flowtype": "^5.2.0",
    "eslint-plugin-jest": "^23.20.0",
    "eslint-plugin-prettier": "^3.1.4",
    "eslint-plugin-react": "^7.20.6",
    "generate-source-map": "0.0.5",
    "jest": "^24.8.0",
    "jest-serializer-html-string": "^1.0.1",
    "lerna": "^4.0.0",
    "nodemon": "^1.19.1",
    "prettier": "^2.2.1",
    "postcss": "^8.3.5"
  }
}
